# Validated runtime deps (these exact versions were installed + tested together).
# Reviewer: `pip install -r requirements.txt && pip install -e .`
#
# CPU-only torch (optional, smaller/faster than the default wheel on Linux):
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu

# ── ingestion + retrieval (validated end-to-end) ─────────────────────────────
sentence-transformers==5.5.1     # dense embeddings + cross-encoder reranker
transformers==5.12.0
torch==2.12.0
numpy<3                          # pin: NumPy-2 ABI must match torch/transformers
qdrant-client==1.13.3            # matches the Qdrant v1.13 server in docker-compose
fastembed==0.8.0                 # BM25 sparse vectors for hybrid search
pypdf==6.13.2
reportlab==4.5.1                 # dataset generator emits .pdf docs

# ── config + CLI ─────────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
PyYAML==6.0.3
typer==0.26.7

# ── API + UI (HTTP layer) ────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
streamlit==1.41.1
