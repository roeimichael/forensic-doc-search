# Runtime deps — mirror of pyproject.toml [project].dependencies (+ UI).
# Reviewer convenience: `pip install -r requirements.txt && pip install -e .`
#
# CPU-only torch (optional, smaller/faster install):
#   pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu

sentence-transformers==3.0.1
qdrant-client==1.10.1
fastembed==0.3.6
pypdf==4.2.0
reportlab==4.2.0
pydantic==2.7.4
pydantic-settings==2.3.4
PyYAML==6.0.1
python-dotenv==1.0.1
typer==0.12.3
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
streamlit==1.36.0
