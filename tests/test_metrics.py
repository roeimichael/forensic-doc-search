"""Metric tests (T4.2): Hit@K and MRR correctness on toy rankings."""

from __future__ import annotations

import pytest


def test_hit_at_k() -> None:
    pytest.skip("scaffold — hit_at_k implemented in a later step (T4.2)")


def test_mrr() -> None:
    pytest.skip("scaffold — mrr implemented in a later step (T4.2)")
